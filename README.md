# task 7
- Feel free to change any thing in the nodes or structure
- when running a node for testing call it with its name in setup.py
### to run a node
#### bash:
- colcon build 
- source  install/setup.bash 
- ros2 run first_pkg node_name